import {
  ConversationsView,
} from "@/components/conversations/conversations-view";

export const metadata = {
  title: "إدارة المحادثات",
};

export default function ConversationsPage() {
  return <ConversationsView />;
}

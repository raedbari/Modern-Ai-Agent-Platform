import { EvaluationDashboard } from "@/components/evaluation/evaluation-dashboard";

export const metadata = {
  title: "التقييم | إدارة المنصة",
};

export default function EvaluationPage() {
  return <EvaluationDashboard />;
}

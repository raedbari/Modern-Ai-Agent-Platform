"""Concrete document parser implementations.

Each module in this package implements the ``DocumentParser`` port for one
file format.  The ``factory`` module provides the ``DefaultParserFactory``
which selects the correct parser by MIME type or extension.
"""
